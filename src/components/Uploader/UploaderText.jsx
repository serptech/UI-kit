import styled from "styled-components";

import { colors } from "../../style";

const UploaderText = styled.p`
  margin: 0;
  color: ${colors.slate};
  font-size: 20px;
  line-height: 40px;
`;

export { UploaderText };
