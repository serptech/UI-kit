import styled from "styled-components";

import { FormDropdownResetButton } from "../FormDropdown/FormDropdownResetButton";

const FormFromToPickerResetButton = styled(FormDropdownResetButton)``;

export { FormFromToPickerResetButton };
