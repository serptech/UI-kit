import styled from "styled-components";

import { Spinner } from "../../../../../Spinner";

const EntryCardPhotoFullImageSpinner = styled(Spinner)`
  position: fixed;
  top: 50%;
  transform: translateY(-50%);
`;

export { EntryCardPhotoFullImageSpinner };
